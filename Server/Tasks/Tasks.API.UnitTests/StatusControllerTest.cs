using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using Tasks.API.Controllers;
using Tasks.Core.Model;
using Tasks.DAL.Data;
using Tasks.DAL.Data.Interfaces;

namespace Tasks.API.UnitTests
{
    [TestClass]
    public class StatusControllerTest
    {
        [TestMethod]
        public void Status_GET_Check()
        {
            //StatusController controller = new StatusController();
            //var expected = controller.Get();
            //var actual = new List<Status>() { new Status { StatusID=1, StatusTypes="В работе" } };

            //Assert.AreEqual(expected, actual, $"Objects are not equal. Expected value is = {expected}. Actual value is = {actual}");
        }
    }
}
